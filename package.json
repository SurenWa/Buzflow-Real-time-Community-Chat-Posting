{
  "name": "buzzline",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:client": "turbo dev --filter=client",
    "dev:server": "turbo dev --filter=server"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "npm@10.9.0"
}
